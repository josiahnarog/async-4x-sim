{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=\"$(jq -r '.tool_input.command // \"\"')\"; case \"$cmd\" in *git\\ push*) echo '{\"continue\": false, \"stopReason\": \"git push blocked — please explicitly ask me to push first\"}'; exit 0 ;; esac",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
